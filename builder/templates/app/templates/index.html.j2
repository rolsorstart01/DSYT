{% extends 'base.html' %}
{% block content %}
<div class="p-5 rounded-3 hero">
  <h1>{{ config.layout.hero_title }}</h1>
  <p class="lead">{{ config.layout.hero_subtitle }}</p>
  <a href="/auth" class="btn btn-primary">Start Trading</a>
</div>
{% endblock %}
