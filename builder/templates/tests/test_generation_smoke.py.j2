from pathlib import Path


def test_files_exist():
    root = Path(__file__).resolve().parents[1]
    assert (root / "run.py").exists()
    assert (root / "app" / "routes.py").exists()
    assert (root / "app_config.json").exists()
